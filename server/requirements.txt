fastapi>=0.109.0,<1.0
uvicorn[standard]>=0.27.0,<1.0
pydantic>=2.5.0,<3.0
httpx>=0.26.0,<1.0
PyJWT>=2.8.0,<3.0

# Ed25519 signing for the threat-feed (Phase 3.1). PyCA `cryptography` ships pre-built
# wheels for Windows/macOS/Linux + manylinux so we don't need libsodium on the host.
cryptography>=42.0.0,<46.0

# Optional production dependencies (only used if env selects redis backends).
redis>=5.0.0,<6.0.0

# Monitoring
prometheus-fastapi-instrumentator>=6.0.0
sentry-sdk[fastapi]>=1.40.0

# Dev / CI
pytest>=8.0.0,<9.0
pytest-asyncio>=0.21.0
